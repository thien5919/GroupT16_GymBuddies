import React, { FC } from "react";
import { View, Text } from "react-native";

const MatchScreen: FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Match</Text>
    </View>
  );
};

export default MatchScreen;
